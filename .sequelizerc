const path = require("path");

module.exports = {
	config: path.resolve(__dirname, "functions", "src", "config", "database.js"),
	"migrations-path": path.resolve(
		__dirname,
		"functions",
		"src",
		"database",
		"migrations"
	),
};
